@model Fitness_Manager.Models.PlanNutritionnel
@{
    ViewData["Title"] = "Modifier Plan Nutritionnel";
    Layout = null;
}

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/css/coach-dashboard.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <div class="dashboard-container">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="logo-section">
                <i class="fas fa-dumbbell"></i>
                <h2>Fitness Manager</h2>
            </div>
            
            <nav class="nav-menu">
                <a href="@Url.Action("Dashboard", "Coach")" class="nav-item">
                    <i class="fas fa-home"></i>
                    <span>Dashboard</span>
                </a>
                <a href="@Url.Action("Clients", "Coach")" class="nav-item">
                    <i class="fas fa-users"></i>
                    <span>Clients</span>
                </a>
                <a href="@Url.Action("WorkoutPlans", "Coach")" class="nav-item">
                    <i class="fas fa-running"></i>
                    <span>Plans d'Entraînement</span>
                </a>
                <a href="@Url.Action("NutritionPlans", "Coach")" class="nav-item active">
                    <i class="fas fa-apple-alt"></i>
                    <span>Plans Nutritionnels</span>
                </a>
                <a href="@Url.Action("Reports", "Coach")" class="nav-item">
                    <i class="fas fa-chart-line"></i>
                    <span>Rapports</span>
                </a>
            </nav>
            
            <div class="sidebar-footer">
                <a href="@Url.Action("Logout", "Account")" class="nav-item">
                    <i class="fas fa-sign-out-alt"></i>
                    <span>Déconnexion</span>
                </a>
            </div>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <header class="dashboard-header">
                <div class="header-content">
                    <div>
                        <a href="@Url.Action("NutritionPlans", "Coach")" class="btn-back">
                            <i class="fas fa-arrow-left"></i> Retour
                        </a>
                        <h1>Modifier Plan Nutritionnel</h1>
                        <p class="header-subtitle">Mettre à jour le plan alimentaire</p>
                    </div>
                </div>
            </header>

            <section class="form-section">
                <div class="form-container">
                    <form method="post" asp-action="EditNutritionPlan">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="Id" />
                        
                        <div class="form-group">
                            <label asp-for="Nom">
                                <i class="fas fa-tag"></i> Nom du Plan *
                            </label>
                            <input asp-for="Nom" class="form-control" placeholder="Ex: Plan Perte de Poids 1500 kcal" required />
                            <span asp-validation-for="Nom" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="Description">
                                <i class="fas fa-align-left"></i> Description
                            </label>
                            <textarea asp-for="Description" class="form-control" rows="4" placeholder="Décrivez l'objectif et les caractéristiques du plan..."></textarea>
                        </div>

                        <div class="form-row">
                            <div class="form-group">
                                <label asp-for="CaloriesJournalieres">
                                    <i class="fas fa-fire"></i> Calories Journalières (kcal)
                                </label>
                                <input asp-for="CaloriesJournalieres" type="number" class="form-control" placeholder="2000" min="500" max="5000" />
                            </div>

                            <div class="form-group">
                                <label asp-for="TypeRegime">
                                    <i class="fas fa-leaf"></i> Type de Régime
                                </label>
                                <select asp-for="TypeRegime" class="form-control">
                                    <option value="">-- Sélectionner --</option>
                                    <option value="Équilibré">Équilibré</option>
                                    <option value="Hyperprotéiné">Hyperprotéiné</option>
                                    <option value="Low Carb">Low Carb</option>
                                    <option value="Végétarien">Végétarien</option>
                                    <option value="Vegan">Vegan</option>
                                    <option value="Paléo">Paléo</option>
                                    <option value="Cétogène">Cétogène</option>
                                </select>
                            </div>
                        </div>

                        <div class="form-group">
                            <label asp-for="Objectif">
                                <i class="fas fa-bullseye"></i> Objectif
                            </label>
                            <select asp-for="Objectif" class="form-control">
                                <option value="">-- Sélectionner --</option>
                                <option value="Perte de poids">Perte de poids</option>
                                <option value="Prise de masse">Prise de masse</option>
                                <option value="Maintien">Maintien</option>
                                <option value="Sèche">Sèche</option>
                                <option value="Santé générale">Santé générale</option>
                            </select>
                        </div>

                        <div class="form-actions">
                            <a href="@Url.Action("NutritionPlans", "Coach")" class="btn-secondary">
                                <i class="fas fa-times"></i> Annuler
                            </a>
                            <button type="submit" class="btn-primary">
                                <i class="fas fa-save"></i> Enregistrer les Modifications
                            </button>
                        </div>
                    </form>
                </div>
            </section>
        </main>
    </div>

    @section Scripts {
        <partial name="_ValidationScriptsPartial" />
    }
</body>
</html>
